# orchestrator
Orchestrator  — Windows app launcher &amp; window manager. Define a list of apps with exact screen positions, then launch or close them all with one click. Supports profiles, hotkeys, scheduler, watchdog, system monitor, and tray. Single portable .exe, no installer.
